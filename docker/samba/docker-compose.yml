version: '3.3'
services:
  samba:
    image: dperson/samba
    container_name: dperson-samba
    restart: always
    ports:
      - 139:139
      - 445:445
    volumes:
      - ./data:/mount
    command: '-u "root;123456" -s "www;/mount/;yes;no;no;all;none"'
